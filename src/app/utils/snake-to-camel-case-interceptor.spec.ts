import { SnakeToCamelCaseInterceptor } from './snake-to-camel-case-interceptor';

describe('SnakeToCamelCaseInterceptor', () => {
  it('should create an instance', () => {
    expect(new SnakeToCamelCaseInterceptor()).toBeTruthy();
  });
});
